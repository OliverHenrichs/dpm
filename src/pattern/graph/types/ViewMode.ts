/**
 * Which visualisation the graph screen is showing.
 *
 * Declared once: this was previously redeclared identically in
 * PatternGraphScreen, GraphViewContainer and PatternGraphHeader.
 */
export type ViewMode = "timeline" | "graph";
